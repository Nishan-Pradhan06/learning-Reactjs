import React from 'react'
// import './App.css'
const Buttons = (props) => {
    return (
        <div>
            <a href="#" className="my-button">{props.ButtonText}</a>
        </div>
    )
}

export default Buttons